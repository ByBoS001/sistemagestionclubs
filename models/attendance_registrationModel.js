class Attendance_registration {
    constructor(idattendance_registration, attendance, iduser, idclub_activity ) {
      (this.idattendance_registration = idattendance_registration),
        (this.attendance = attendance),
        (this.iduser = iduser),
        (this.idclub_activity = idclub_activity);
    }
  }
  
export default Attendance_registration;